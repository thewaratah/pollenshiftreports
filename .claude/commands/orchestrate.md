Use the `shift-report-orchestrator` sub-agent to fully coordinate and execute this multi-step task end-to-end:

$ARGUMENTS

The orchestrator will plan, dispatch specialist agents in parallel (Sakura + Waratah streams), review results, and report back. Suitable for: new features spanning both venues, large refactors, or anything requiring more than 2 specialist agents.
