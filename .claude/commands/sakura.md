Use the `sakura-gas-agent` sub-agent to handle this Sakura House task:

$ARGUMENTS

The agent will read CLAUDE_SAKURA.md first, then implement or investigate as needed. It specialises in named-range architecture, 6-day operation (Mon–Sat), and all Sakura-specific patterns.
