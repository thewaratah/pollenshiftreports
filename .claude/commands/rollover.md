Use the `rollover-trigger-agent` sub-agent to handle weekly rollover or trigger management.

Task: $ARGUMENTS

The agent handles: running/previewing rollover, creating/removing time-based triggers, verifying rollover state, and debugging rollover failures for both Sakura House and The Waratah.
