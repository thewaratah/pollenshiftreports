Use the `documentation-agent` sub-agent to update project documentation.

What changed (or leave blank for auto-detect): $ARGUMENTS

The agent will make minimal, targeted updates to whichever CLAUDE.md files are affected: CLAUDE.md, CLAUDE_SAKURA.md, CLAUDE_WARATAH.md, or CLAUDE_SHARED.md. It does not rewrite — it patches only what is inaccurate or missing.
