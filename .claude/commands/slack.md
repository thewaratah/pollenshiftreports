Use the `slack-block-kit-agent` sub-agent to design or debug a Slack Block Kit message.

Task: $ARGUMENTS

The agent specialises in bk_header / bk_section / bk_fields / bk_list / bk_buttons / bk_context / bk_post patterns as implemented in this project. It can design new messages, debug webhook failures, or review existing Block Kit output.
