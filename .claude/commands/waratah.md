Use the `waratah-gas-agent` sub-agent to handle this Waratah task:

$ARGUMENTS

The agent will read CLAUDE_WARATAH.md first, then implement or investigate as needed. It specialises in hardcoded-cell architecture, 5-day operation (Wed–Sun), and all Waratah-specific patterns.
