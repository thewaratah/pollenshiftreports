Use the `gas-code-review-agent` sub-agent to review the recently changed files.

If specific files are provided, review those: $ARGUMENTS

Otherwise review all files modified in the current session. Apply the full P0–P3 severity checklist. Return a structured report with blocking issues (P0/P1) listed first.
