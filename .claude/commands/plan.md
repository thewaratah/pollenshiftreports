Use the `shift-report-orchestrator` sub-agent to plan and coordinate this task:

$ARGUMENTS

The orchestrator will:
1. Read the appropriate CLAUDE.md guides
2. Break the work into parallel Sakura + Waratah streams where applicable
3. Identify which specialist agents to dispatch
4. Apply the 80/20 rule — plan only what's needed, then hand off to implementation agents

Do not write any code in this step — produce a plan for approval first.
